/**
 * Supabase configuration for the Support Application Portal.
 *
 * Copy this file to supabase-config.js and replace the placeholder values
 * with your project URL and anon (public) key from the Supabase dashboard.
 *
 * Never commit supabase-config.js with real keys to a public repository
 * if the key is not restricted by RLS (row-level security is enabled on the table).
 */
window.SUPABASE_CONFIG = {
  url: "YOUR_SUPABASE_URL",
  anonKey: "YOUR_SUPABASE_ANON_KEY",
};
