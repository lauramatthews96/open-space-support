/**
 * External Resources page - all organisations, grouped by category.
 */
(function () {
  var mount = document.getElementById("resources-external");
  if (!mount || !window.renderResourcesExternalSection) return;

  window.renderResourcesExternalSection("");
})();
